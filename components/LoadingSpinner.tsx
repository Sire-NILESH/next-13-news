import { Spinner } from "@nextui-org/spinner";
import React from "react";

const LoadingSpinner = () => {
  return <Spinner size="lg" className="h-full w-full mx-auto" />;
};

export default LoadingSpinner;
